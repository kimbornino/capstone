@model IEnumerable<Capstone.Models.RecipeMatch>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Food)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FeaturedIngredient)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Food.FoodID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FeaturedIngredient)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RecipeMatchID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RecipeMatchID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RecipeMatchID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
